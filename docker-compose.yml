version: "3.9"

services:

  laravel:

    build: .

    container_name: la-confianza

    restart: always

    ports:
      - "8000:8000"

    env_file:
      - .env

    volumes:
      - .:/var/www/html